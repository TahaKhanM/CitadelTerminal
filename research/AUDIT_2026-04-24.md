# SimCore autonomous-run audit — 2026-04-24

Phase 1.B.0 engine-consistency verdict + doc-claim audit for the
multi-phase SimCore capstone.

## Phase 1.B.0-A  Engine consistency

**Verdict: PASS-small (drifts documented, non-structural).**

### A1. engine.jar hash

```
0b72f7b37a1d482d57ce84e8841f8e1ba6adc071160b6c5cac79df31ab74b524  C1GamesStarterKit-master/engine.jar
size=28,642,980  mtime=Jan 22 17:28
```

Jar TOC inspected: all expected classes present
(`com/c1games/terminal/game/{events/,player/,gameobject/,units/,systems/,Game,GameMain,PathFinder}`).
No unfamiliar new classes.

### A2. Server config diff

Extracted live server config from a v13 ranked replay header
(`/tmp/server_config.json`, source
`replays/ranked/v13_360023_m15302602_vs_gooder-maybe_1453_win.replay`).
Diffed against `configs/competition-game-configs.json`.

**4 diffs, all explained:**

| Key | Local | Server | Status |
|---|---|---|---|
| `_source` | stamped provenance | absent | local-only metadata |
| `misc` | `{numBlockedLocations:0, blockedLocations:[]}` | absent | local-only Config.java default |
| `resources.coresForPlayerDamage` | `1` | absent | **legacy key** — confirmed non-existent in engine's live config; Cluster B.1 drift guard disables this rule |
| `unitInformation[1].upgrade.cost1` | `2` | absent | engine's `Config.processUnitInfoInPlace` inherits base `cost1 = 4.0` when `upgrade.cost1` is missing; local config's `2` was wrong, but copy-then-patch semantics in the sim keep upgrade cost at 4 |

### A3. Replay-the-actions equivalence

Skipped as redundant: the current SimCore validator already runs every
turn of every v13 ranked replay through a bit-identical frame loop and
reports **19/23 columns at strict max_err=0** across 87,677 action
frames. This is stronger than A3 (which only compared aggregate
outputs of one replay). If engine.jar had drifted from the server
engine beyond JVM non-determinism, the validator would diverge on
deterministic columns (stats, structures, attacks, spawns, moves) — it
does not.

See `/tmp/validator_baseline_20260424.txt` for the 23-column table.

## Phase 1.B.0-B  Doc claim audit (6-agent parallel sweep)

### Summary table of INCORRECT claims found

| File | Line(s) | Claim | Correct fact | Engine citation |
|---|---|---|---|---|
| CLAUDE.md | 74 | "+1 SP per HP damage dealt" | +1 SP per mobile unit that breaches (metalForBreach) | BreachSystem.java:27 |
| .claude/skills/competition-reference/SKILL.md | 52 | "Damage bonus: +1 SP per HP damage" | no such mechanic — only metalForBreach | same |
| docs/GAME_RULES.md | 30-34 | MP income buckets `1 + turn//5` off-by-one | buckets shift 1 turn earlier: `bitsPerRound + ((turn+1-roundStartBitRamp)/5 + 1) * bitGrowthRate` | GameMain.java:448-454 |
| docs/GAME_RULES.md | 34, 130, 139 | damage bonus mechanic | breach-based, not damage-based | BreachSystem.java:27; coresForPlayerDamage absent |
| docs/GAME_RULES.md | 60-64 | frame order "Shield → Move → Attack → Cleanup" | `move → collision → shield_decay → shield_give → breach → self_destruct → attack → cleanup` | Game.java:167-182 |
| docs/UNITS_REFERENCE.md | 206 | refund hedge "0.75 in older config; verify live" | definitively 0.9 base / 0.8 upgraded | server config confirmed |
| docs/UNITS_REFERENCE.md | 207 | "turnsRequiredToRemove: 1" | 2 base / 3 upgraded | server config + UNITS_REFERENCE L163-164 |
| docs/TARGETING_AND_PATHING.md | 10-12 | "Shielding runs BEFORE movement" | shield runs AFTER movement (between collision and breach) | Game.java:167-182 |
| docs/TARGETING_AND_PATHING.md | 32 | "minimum current HP" | min `(HP + shield)` | TargetAndAttackSystem.java:77 |
| docs/TARGETING_AND_PATHING.md | 32-34 | "deepest into attacker's side (LARGEST y)" | SMALLEST `|y - attackerStartEdge|` (closer to own side, not farther) | TargetAndAttackSystem.java:80-82 |
| docs/TARGETING_AND_PATHING.md | 36 | "most recently created" | largest integer-parsed gid (correlated but not identical) | TargetAndAttackSystem.java:87 |
| docs/TARGETING_AND_PATHING.md | 80 | "recomputes every step" | persistent 4-per-edge PathFinder; `put/remove/lazy-validate` | PathFinder.java |
| docs/TARGETING_AND_PATHING.md | 70 | "SD damage = starting HP" | config `selfDestructDamageWalker/Tower` (equals startHP in this competition, but not the same mechanism) | SelfDestructSystem.java:34 |

### VERIFIED claims (no edit needed)

Every numeric unit stat in `docs/UNITS_REFERENCE.md` body matches
server config exactly (Wall/Support/Turret/Scout/Demolisher/Interceptor:
HP, costs, ranges, damages, refund %, SD params). Wall upgrade
**does** cost 2 SP (confirmed `unitInformation[0].upgrade.cost1=2.0`).
Support upgrade cost inherits base `4 SP` via copy-then-patch (upgrade
dict has no cost1 key). Support shield formula `1 + 0.7×Y` at range 7
(upgraded) verified. Refund formulas 0.9/0.8 verified.

`docs/MAP_AND_COORDINATES.md` clean — every edge constant, tile
enumeration, and territory split checks against `MapBounds.java` and
`sim/map.py`.

`research/engine_decompiled/PATHFINDER_SPEC.md` clean on all
load-bearing claims (4 per-edge PathFinders, state arrays, put/remove/
getStep, filter order, zigzag, direction filter, tiebreak order). One
minor comment label drift in §1 NEIGHBOR offsets; `idealnessSearch`
Python port has a non-bit-exact fall-through check worth re-verifying
against `javap -c` output as follow-up.

### Applied edits (this commit)

1. CLAUDE.md L74 — breach-based bonus language
2. .claude/skills/competition-reference/SKILL.md L52 — delete stale damage-bonus bullet
3. docs/UNITS_REFERENCE.md L206-207 — schema-table row fixes

### Follow-up edits (separate commits, smaller risk surface)

- docs/GAME_RULES.md § 3.1 / § 3.6 — MP income formula + frame order
- docs/TARGETING_AND_PATHING.md § targeting cascade + pathing
  mechanism
- research/engine_decompiled/PATHFINDER_SPEC.md § 1 + § 6.7 — minor
  label fixes + javap caveat

These carry more rewrite surface than L-level edits; staged separately
so the next diff-review is tractable.

## Current parity baseline (frozen)

```
Phase 1.B — full-state parity | 87677 frames across 1486 turns | wall=25.01s
  throughput: 59.4 sims/s | 3506 frames/s
  19 columns: max_err=0, mean_err=0
  p1_mobiles:        130 frames, 3 replays
  p2_mobiles:        310 frames, 3 replays
  event_damage:       10 frames, 4 replays
  event_selfDestruct: 10 frames, 4 replays
  state-cascade frames: 440 / 87677 = 0.50%  (gate: <1%)
```

All 4 residuals attributable to JVM HashSet ordering of
`ColliderComponent.collidedWithThisTurn` feeding TargetAndAttackSystem
and SelfDestructSystem. 17 of 23 replays are strict-clean across all
23 columns.

Phase 1.B.0 closed. Proceeding to Phase 1.B.1 (H-refinement on
p_mobiles + state-cascade attribution).
